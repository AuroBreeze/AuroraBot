version: "3"
services:
    aurorabot:
        environment:
            - ACCOUNT=3552638520
        ports:
            - 3001:3001
            - 6099:6099
        container_name: Bot
        restart: always
        image: mlikiowa/napcat-docker:latest
    app:
        build: .
        restart: always
        container_name: AuroraBot
        depends_on:
            - aurorabot
        image: aurora-bot:latest
